import React from 'react';
import {
  View,
  StyleSheet
} from 'react-native';

const PinDetailFull = () => {
  return (
    <View style={styles.container} />
  );
}

const styles = StyleSheet.create({
  container: {
    position: 'absolute',
    height: '100%',
    width: '100%',
    backgroundColor: '#151515c3'
  }
});

export default PinDetailFull;
